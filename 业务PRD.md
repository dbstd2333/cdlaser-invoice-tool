# 成都莱盛发票库存管理工具业务 PRD

## 1. 文档信息

- 产品形态：Windows 单机桌面应用
- 使用角色：单名财务人员，无登录流程
- 核心目标：管理客户、商品价格版本、开票记录和可正可负的发票库存
- 目标版本：V1.0

## 2. 背景与目标

财务当前通过多个 Excel 维护客户、商品、销项开票和总部进项票，存在数据分散、重复录入、库存口径不统一、历史变更难追踪等问题。

系统需要完成以下目标：

1. 月中按客户选择多个商品价格版本，导出税务系统项目明细模板并保存开票记录。
2. 月底只导出当前负库存商品，向总部索取对应进项票。
3. 月初导入总部进项票，增加库存并保存进项批次及库存流水。
4. 快速查询每个商品价格版本当前是有余量、已平衡还是待补票。
5. 对客户、商品、价格、库存、开票、导入和作废提供可追溯的字段级历史。

## 3. 非目标

V1 不包含：

- 多用户、账号、角色和权限管理。
- 与电子税务局 API 直连或自动提交发票。
- 自动拆分多张发票。
- 采购、实物仓库、批次、库位和成本核算。
- 移动端、Web 端和多人同时编辑。
- 自动获取税收分类编码。

## 4. 核心业务定义

### 4.1 商品

商品由“项目名称 + 规格型号”唯一识别，包含单位、商品和服务税收分类编码、启用状态等资料。

同一商品可以有多个价格版本。单位或税收分类编码变化属于商品资料冲突，不自动覆盖。

### 4.2 价格版本

价格版本属于某个商品，主要字段为不含税单价。相同商品、相同单价只能有一个有效价格版本。

- 单价最多保留 13 位小数。
- 开票时只能选择已有价格版本。
- 选入开票清单后单价不可修改。
- 已被业务记录引用的价格版本不可改价，只能停用并新增版本。
- 不同价格版本独立统计库存和月底负库存导出。

### 4.3 库存

系统统一使用一个有符号整数“库存”：

- `库存 > 0`：有可用余量。
- `库存 = 0`：已平衡、无余量。
- `库存 < 0`：待总部补票，待补数量为库存绝对值。

库存变化规则：

- 销项开票：库存减少。
- 总部进项票导入：库存增加。
- 销项作废：恢复原扣减数量，库存增加。
- 进项作废：冲销原增加数量，库存减少。
- 人工调整：按填写的正负调整量变化，并强制填写原因。

开票时不校验库存是否足够，也不阻止库存变为负数。

### 4.4 开票记录

每次面向一个客户导出一份税务模板，即生成一条开票记录和多条商品明细。

记录保存导出时的客户、商品、型号、单位、税收分类编码、价格、数量、金额和税率快照。后续修改基础资料不得改变历史记录。

### 4.5 时间口径

开票记录按系统成功导出税务 Excel 的本地时间记录。月底负库存导出以点击导出时的实时库存快照为准，不根据开票记录日期重新汇总。

### 4.6 导入导出分类

系统的数据初始化和总部补票交换严格分为五类：

1. 客户首次导入：只用于初始化客户，成功后隐藏。
2. 商品首次导入：导入商品、价格版本和初始库存，成功后隐藏。
3. 商品日常导入：只增加新商品或新价格版本，不修改库存。
4. 商品月底导出：只导出当前库存小于 0 的价格版本。
5. 商品月初导入：导入总部进项票并增加库存。

月中面向客户导出的税务项目明细属于“销项开票”，是独立业务动作，不属于上述五类数据初始化和总部补票交换。

## 5. 信息架构

系统有三个一级页面。

### 5.1 整体布局

采用 Element Plus 传统 B 端“左侧菜单 + 右侧内容”布局：

- 左侧为全高一级菜单，展开宽度约 220px，折叠后约 64px。
- 左上展示系统名称和图标，侧栏提供明确的折叠/展开按钮。
- 左侧一级菜单依次包含“客户管理”“商品、库存与开票”和“开票记录”。
- “设置”固定在左侧菜单最下方，与上方业务菜单分隔，不随内容滚动。
- 当前菜单项始终高亮，菜单状态与页面路由保持一致。
- 每次启动、异常重启或备份恢复重启后，默认进入页面二“商品、库存与开票”并高亮对应菜单。
- 系统不记忆上次关闭时停留的页面；页面一和页面三只能由财务主动点击进入。
- 右侧由顶部工具栏和主内容区组成。
- 顶部工具栏显示菜单折叠按钮、面包屑和当前页面名称。
- 主内容区使用浅灰背景，业务内容放入白色卡片，统一留白和滚动行为。
- 页面一不使用子页签，主区域始终显示客户大表格；CRUD 和首次批量导入使用 Modal。
- 页面二不使用子页签，主区域始终显示商品价格版本与库存大表格。
- 页面二的开票、导入、导出和流水等子功能统一使用大尺寸 Modal。
- 页面三不使用子页签，主区域始终显示开票记录大表格；点击记录后使用大尺寸 Modal 展示该次开票明细。
- 点击左侧菜单左下角“设置”后打开右侧抽屉，腾讯云 COS 备份恢复不占用主内容路由。
- 菜单折叠时设置只显示齿轮图标，悬停显示“设置”提示。
- 页面一、页面二和页面三的大表格默认每页显示 50 条，可切换为 20、50 或 100 条。

窗口宽度不足 1280px 时默认折叠左侧菜单；系统不针对手机端设计。

### 5.2 页面一：客户管理

页面一是单一客户大表格工作台，不切换子路由或页签。

表格上方包括：

- 搜索框：客户名称、简码、纳税人识别号、电话和银行账号。
- 筛选项：客户状态、是否默认地址和资料完整度。
- 操作区：新增客户、客户首次批量导入、刷新、重置筛选和列显示设置。
- “客户首次批量导入”仅在首次导入尚未完成时显示。

主表格显示：

- 客户名称、纳税人识别号和简码。
- 地址、电话、开户行和银行账号。
- 联系邮箱、默认地址标记、状态和最近更新时间。
- 行操作：查看、编辑、停用/恢复、字段历史。

表格使用固定表头、固定右侧操作列和分页加载。

页面一配套 Modal：

- 新增/编辑客户 Modal：显示完整客户表单。
- 客户详情 Modal：只读展示客户资料及关联开票摘要。
- 客户首次批量导入大 Modal：完成文件选择、解析、错误校验、数据预览和确认导入。
- 字段历史大 Modal：按字段和时间查看变更前后值。

CRUD Modal 建议宽度 760px；包含大表格的导入和历史 Modal 使用约 `90vw × 88vh`。同一时间只允许打开一个客户业务 Modal，存在未保存表单时关闭需要二次确认。

### 5.3 页面二：商品、库存与开票

页面二是单一大表格工作台，不切换子路由或页签。

表格上方包括：

- 搜索框：项目名称、型号和税收分类编码。
- 筛选项：库存状态、资料状态、启用状态和单价。
- 操作区：销项开票、新增商品、商品导入、月底导出、月初进项、导入记录及库存流水。
- 刷新、重置筛选和列显示设置。

主表格一行对应一个商品价格版本，显示：

- 多选框。
- 项目名称、型号、单位和税收分类编码。
- 不含税单价。
- 当前库存及状态。
- 资料状态、启用状态和最近变更时间。
- 行操作：查看历史、编辑资料、停用/启用、库存调整。

库存小于 0 使用风险色并显示“待补 N”；等于 0 显示“已平衡”；大于 0 显示“有余量 N”。表头固定，操作列固定在右侧，数据使用分页加载，默认每页 50 条。

商品勾选采用跨分页保留策略：

- 勾选以商品价格版本为最小单位，同一项目名称和型号的不同单价版本分别勾选。
- 翻页、改变每页条数或调整搜索筛选条件时，已勾选项仍保留。
- 表格上方持续显示“已选 N 项”，并提供“查看已选”和“清空已选”。
- 被停用、删除或资料变为不完整的价格版本必须从已选项中移除并明确提示。
- 离开页面二或完成一次开票后清空选择；取消销项开票 Modal 不自动清空，便于继续调整。

### 5.4 页面二大 Modal 规范

页面二所有子功能在大尺寸 Modal 中完成：

- 销项开票 Modal。
- 商品新增/编辑 Modal。
- 商品首次导入和日常导入 Modal。
- 商品月底负库存导出 Modal。
- 商品月初总部进项导入 Modal。
- 导入记录及详情 Modal。
- 库存流水和字段历史 Modal。
- 库存调整 Modal。

统一交互规则：

- 默认宽度约为窗口的 90%，高度不超过窗口的 88%；小窗口可切换全屏。
- 标题和关闭按钮固定在顶部，主要操作按钮固定在底部，内容区独立滚动。
- 同一时间只打开一个业务 Modal，不叠加多个大 Modal。
- 二次确认使用轻量确认框，不再嵌套另一个大 Modal。
- Modal 内的表格保持固定表头，并显示明确的加载、空数据和错误状态。
- 存在未提交数据时关闭 Modal，必须提示是否放弃。

### 5.5 页面三：开票记录

页面三是独立的开票记录大表格工作台，不切换子路由或页签。

表格上方包括：

- 搜索框：开票批次号、客户名称和商品名称/型号。
- 筛选项：Excel 导出时间范围、记录状态。
- 操作区：刷新、重置筛选和列显示设置。

主表格一行对应一次税务 Excel 导出，显示：

- 开票批次号、客户名称、Excel 导出时间和状态。
- 商品明细行数、数量合计、金额、税额和价税合计。
- 行操作：查看明细、重新下载 Excel；有效记录还可作废并恢复库存。

表格使用固定表头、固定右侧操作列和分页加载，默认每页 50 条。点击记录行或“查看明细”后打开大尺寸明细 Modal；点击行内下载、作废按钮时只执行对应操作，不触发行点击。

开票记录明细 Modal 展示：

- 批次号、客户快照、Excel 导出时间、状态和金额汇总。
- 每条商品价格版本快照，包括项目名称、型号、单位、单价、税率、数量、金额、税额和价税合计。
- 每条明细的“扣减前库存 → 扣减后库存”，例如 `3 → -2`；该值取导出成功时保存的不可变快照，不按当前库存反推。
- 已作废记录的作废时间、作废原因和库存恢复结果。
- 重新下载首次生成的原始 Excel；有效记录可在二次确认后作废。

记录及明细不可编辑、不可物理删除。明细 Modal 遵循页面二大 Modal 的尺寸、滚动和固定表头规范。

备份、恢复和系统信息从左侧菜单左下角“设置”打开，不新增一级页面。

## 6. 数据字段

### 6.1 客户字段

| 字段 | 必填 | 规则 |
| --- | --- | --- |
| 客户分类名称 | 否 | 默认“客户分类” |
| 客户名称 | 是 | 最多 100 字符 |
| 纳税人识别号 | 是 | 全局唯一，按文本保存 |
| 简码 | 否 | 用于搜索 |
| 地址 | 否 | 文本 |
| 电话 | 否 | 按文本保存 |
| 开户行名称 | 否 | 文本 |
| 银行账号 | 否 | 按文本保存，不允许科学计数法入库 |
| 联系邮箱 | 否 | 格式校验 |
| 是否默认地址 | 否 | 是/否 |
| 状态 | 是 | 启用/停用 |

客户已有开票记录时只能停用，不能物理删除。

### 6.2 商品字段

| 字段 | 必填 | 规则 |
| --- | --- | --- |
| 项目名称 | 是 | 不含税收分类简称及星号内容 |
| 规格型号 | 是 | 按文本保存，保留前导零 |
| 单位 | 是 | 最多 20 字符 |
| 税收分类编码 | 是 | 最多 19 字符，按文本保存 |
| 状态 | 是 | 完整、资料待补、停用 |
| 备注 | 否 | 文本 |

总部进项票自动创建的新商品如果没有税收分类编码，状态为“资料待补”，不能用于销项导出，补齐后方可启用。

### 6.3 价格版本字段

| 字段 | 必填 | 规则 |
| --- | --- | --- |
| 所属商品 | 是 | 商品引用 |
| 不含税单价 | 是 | 大于 0，最多 13 位小数 |
| 税率 | 是 | 固定 0.13，不可编辑 |
| 当前库存 | 是 | 有符号整数 |
| 状态 | 是 | 启用/停用 |

## 7. 详细业务流程

### 7.1 客户首次导入

1. 客户尚未完成初始化时，客户页面显示“客户首次导入”。
2. 财务点击后打开客户首次批量导入大 Modal。
3. 文件字段按客户信息模板解析。
4. Modal 展示新增数量、重复税号、格式错误和客户数据预览。
5. 任一行错误则整批失败，不写入任何客户。
6. 首次成功后关闭 Modal 并隐藏入口，后续客户仅通过单条 CRUD 维护。
7. 客户首次导入记录和字段历史永久保留。
8. 税号、电话和银行账号按文本解析；发现已丢失精度的科学计数值时阻止导入。

### 7.2 商品首次导入

1. 首次启动且未完成初始化时，在“商品与库存”显示“初始化导入”。
2. 财务下载系统模板，填写商品、型号、单位、税收分类编码、单价和初始库存。
3. 初始库存允许正数、0 或负数，必须为整数。
4. 系统解析后展示新增商品数、价格版本数、库存合计及错误明细。
5. 任一行错误则整批失败，不写入任何数据。
6. 财务二次确认后一次性写入商品、价格版本、库存流水和初始化记录。
7. 首次成功后隐藏初始化入口，不能再次初始化。
8. 后续错误通过“库存调整”修正，不重新执行初始化。

客户首次导入与商品首次导入使用两个独立完成标记，互不影响入口显示。

### 7.3 商品日常导入

- 商品首次导入成功后显示，之后长期保留。
- 可新增商品及价格版本。
- 已有商品出现新单价时创建新价格版本。
- 已有商品的单位或税收分类编码不一致时整批失败，提示人工处理。
- 不允许通过日常导入覆盖已有商品或价格版本字段。
- 日常商品导入不得修改当前库存。
- 文件内容完全相同时阻止重复导入。

### 7.4 客户日常管理

- 支持按客户名称、简码、税号和状态搜索。
- 新增或编辑时实时校验税号唯一性。
- 客户停用后不能被新开票选择，但历史记录仍可查看。

### 7.5 月中销项开票

1. 财务在页面二大表格中跨分页勾选价格版本并点击“销项开票”。
2. 系统打开销项开票大 Modal，并带入已选价格版本。
3. 财务选择一个启用客户，也可继续在 Modal 中搜索并补选商品。
4. 商品以价格版本为可选单元，显示项目名称、型号、单位、单价和当前库存。
5. 每行填写正整数数量；单价和税率不可修改。
6. 系统计算：
   - 金额 = 数量 × 不含税单价，四舍五入保留 2 位。
   - 税额 = 金额 × 13%，四舍五入保留 2 位。
   - 价税合计 = 金额 + 税额。
7. 导出前显示客户、行数、数量合计、金额、税额和价税合计。
8. 单次最多选择 2,000 条明细；超过时阻止导出，不自动拆单。
9. 财务选择 Windows 保存位置并确认导出。
10. 系统以官方税务模板生成一份 Excel，不拆分发票。
11. 只有文件生成与保存成功后，系统才保存开票记录并逐行减少库存。
12. 任一步骤失败时不得产生开票记录或库存变更。

税务模板写入规则：

- 从“1-明细模板”第 4 行开始写入。
- 写入项目名称、税收分类编码、规格型号、单位、数量、单价、金额和税率 `0.13`。
- 折扣、优惠政策、煤炭及特定业务字段留空。
- 保留原模板所有工作表、隐藏数据、版本信息、格式和说明。

### 7.6 开票记录与作废

页面三的开票记录列表支持按批次号、客户、Excel 导出时间、状态和商品关键词查询，默认每页 50 条。

可执行：

- 点击记录查看开票批次及商品快照。
- 查看每个商品价格版本在本次开票中的“扣减前库存 → 扣减后库存”。
- 重新下载首次生成的原始 Excel。
- 作废有效记录。

作废规则：

1. 只能作废一次。
2. 必须填写作废原因并二次确认。
3. 作废后逐行增加相同数量库存。
4. 原记录、原 Excel 和全部历史永久保留。
5. 作废会立即改变商品库存，因此下一次月底负库存导出结果也会变化。

### 7.7 商品月底导出

1. 数据源是导出瞬间所有 `库存 < 0` 的启用价格版本。
2. 库存等于 0 或大于 0 的商品一律不导出。
3. 导出数量取库存绝对值，例如库存 `-12` 导出数量 `12`。
4. 不按客户、开票记录或开票日期拆分。
5. 不同价格版本独立成行，不合并单价。
6. 导出列为：项目名称、型号、单位、待补数量、不含税单价、不含税金额、税率、税额、价税合计。
7. 金额按待补数量和价格版本单价重新计算。
8. 页尾给出数量、金额、税额和价税合计总计。
9. 每次导出保存负库存快照和原始 Excel，支持查看历史及重新下载。
10. 导出本身不改变库存；没有负库存时提示“当前无需向总部补票”，不生成空文件。

### 7.8 商品月初导入（总部进项票）

1. 财务选择总部 Excel。
2. 系统计算原文件哈希和标准化业务内容哈希，任一已存在则阻止重复导入。
3. 识别开票日期、发票号、销售方名称、品名、型号、单位、数量、单价、金额、税额和合计。
4. 同一文件中相同“商品 + 型号 + 单位 + 单价”的行先合并。
5. 运输费等型号、单位和数量均为空的非库存费用行自动忽略，并在预览中列出。
6. 部分填写或数量不是正整数的行属于错误，整批失败。
7. 已有商品必须匹配单位和既有价格版本：
   - 单位不一致或出现未建档的新单价时，整批失败，提示人工确认。
   - 当前库存小于 0 时，导入数量不得超过库存绝对值；超过则整批失败并提示超出数量。
   - 当前库存大于等于 0 时，允许继续增加库存。
8. 系统从未出现的新商品可自动创建商品和价格版本，并按导入数量形成正库存。
9. 新商品缺少税收分类编码时标记为“资料待补”。
10. 预览确认后一次性保存进项批次、行明细、库存增加流水和字段历史。

进项批次支持作废：必须填写原因，整批反向扣减库存，原始文件和历史永久保留。

### 7.9 库存查询与调整

支持筛选：

- 有库存：大于 0。
- 已平衡：等于 0。
- 待补票：小于 0。
- 项目名称、型号、单位、单价、资料状态和启用状态。

列表显示每个价格版本的当前库存和最近变更时间。

人工调整必须填写非零整数调整量和原因，生成独立库存流水，不允许直接覆盖库存值。

### 7.10 字段级历史

客户、商品、价格版本和库存字段提供类似多维表格的历史入口。

每条历史至少显示：

- 变更时间。
- 业务动作。
- 字段名称。
- 变更前值和变更后值。
- 来源批次或关联记录。
- 操作者固定显示“本机财务”。

导入、导出、作废、人工调整、备份恢复及系统迁移也必须记录操作事件。

## 8. 云备份与恢复

腾讯云 COS 备份与恢复放在左侧菜单左下角“设置”触发的独立页签中，不新增一级页面。抽屉从右向左打开，建议宽度 720px，并在小窗口下不超过可用内容宽度。

### 8.1 页面结构

设置抽屉包含“COS 备份与恢复”页签，按以下区域从上到下排列：

1. 备份状态卡片。
2. COS 连接配置。
3. 备份策略。
4. 操作按钮。
5. 云端备份历史。

### 8.2 备份状态卡片

显示：

- 当前状态：未配置、连接正常、连接异常、正在备份或正在恢复。
- 最近一次成功备份时间。
- 最近一次备份大小。
- 当前是否存在未备份的数据变更。
- 最近一次失败原因及“重试”入口。

### 8.3 腾讯云 COS 连接配置

支持腾讯云对象存储 COS，字段包括：

- Region，例如 `ap-guangzhou`。
- Bucket，必须使用包含 APPID 的完整名称，例如 `invoice-backup-1250000000`。
- 对象路径前缀 Prefix。
- SecretId。
- SecretKey，密码框显示，默认不回显原值。
- SecurityToken，使用临时密钥时填写。
- 恢复密码及确认密码，用于备份文件加密。

提供“测试连接”按钮。测试成功后显示 COS Bucket 连接成功；失败时给出可理解的原因，不暴露密钥。

未通过连接测试时允许保存草稿配置，但不能开启自动备份。

### 8.4 备份策略

- 自动备份开关。
- 默认数据发生变化后标记待备份，并每日自动备份一次。
- 默认保留最近 30 个成功版本。
- 页面显示策略说明和下一次预计备份条件。

### 8.5 操作与反馈

- “保存配置”：校验并保存当前设置。
- “立即备份”：创建数据库一致性快照、加密并上传。
- “刷新列表”：重新读取云端备份。
- 备份期间显示阶段、进度和已上传大小，防止重复点击。
- 关闭设置抽屉不取消正在执行的备份。
- 备份失败不影响本地业务，支持重试。

### 8.6 云端备份历史

列表显示：

- 备份时间。
- 文件大小。
- 应用版本。
- 数据库 Schema 版本。
- 备份类型：自动/手动。
- 校验状态。
- 操作：恢复。

默认按时间倒序，支持刷新和加载更多。

### 8.7 恢复流程

1. 财务点击某个备份的“恢复”。
2. 弹出高风险确认框，显示备份时间、版本、大小及“恢复会替换当前数据”的提示。
3. 输入恢复密码并再次确认。
4. 系统先创建当前数据的本地安全备份。
5. 下载云端文件并完成哈希、解密、SQLite 完整性和版本兼容校验。
6. 任一校验失败则停止恢复，当前数据库保持不变。
7. 校验通过后替换数据库并自动重启应用。
8. 重启后显示恢复结果，并记录恢复审计事件。

COS 密钥和恢复密码不得出现在导出文件、日志或业务数据库明文中。

## 9. 异常与提示

- 导入采用“先预览、后确认、全量成功或全量失败”。
- 冲突提示必须定位到工作表、行号、字段、原值和原因。
- 文件被占用、保存失败、模板损坏或磁盘空间不足时不得改变业务数据。
- 应用异常退出后再次启动，要自动检查数据库、未完成任务和孤立临时文件。
- 删除、作废、恢复和库存调整均需二次确认。

## 10. 验收标准

1. 客户首次导入成功后入口隐藏，客户 CRUD、停用和历史查询正常。
2. 可维护同一商品的多个不可编辑价格版本。
3. 销项导出后库存准确减少，允许变为负数。
4. 税务模板可被 Excel 正常打开，结构和隐藏工作表未丢失。
5. 可按原始内容重新下载任意开票文件。
6. 作废销项后库存完整恢复，并影响后续月底负库存导出。
7. 商品月底导出仅包含负库存，数量等于库存绝对值。
8. 总部文件重复导入、冲突、超量或部分错误时整批无写入。
9. 新商品进项可自动建档并增加库存。
10. 所有库存变化均能由库存流水重算并与当前库存一致。
11. 字段级历史可还原每次变更前后值。
12. COS 备份可成功上传、校验、下载并恢复到可用状态。
13. 可在设置中完成 COS 配置、测试、立即备份、查看历史和安全恢复，无需离开当前一级页面。
14. 全局使用左侧菜单、右侧内容的传统 B 端布局；设置固定在菜单左下角，折叠、路由高亮和抽屉行为一致。
15. 页面二始终展示商品库存大表格，所有子功能通过大尺寸 Modal 完成且不使用页签。
16. 页面一始终展示客户大表格，CRUD 和客户首次批量导入均通过 Modal 完成且不使用页签。
17. 每次应用启动均默认进入页面二，不恢复上次停留页面。
18. 开票记录作为页面三独立展示，点击任一记录可查看商品明细及逐行“扣减前库存 → 扣减后库存”快照。
19. 页面二商品勾选在翻页、改变每页条数和调整筛选条件后仍保留，并能查看或清空全部已选项。
20. 页面一、页面二和页面三的大表格默认每页 50 条。
